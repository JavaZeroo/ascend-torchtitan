# torchtitan's requirements.txt minus CUDA-only extras (attn-gym[linear]).
# Keep in sync with <torchtitan>/.ci/docker/requirements.txt at the pinned SHA.
grain
torch_checkpointing>=0.1.0
datasets>=3.6.0,<4.8.0
tokenizers>=0.15.0
safetensors
tyro>=1.0.5
tensorboard
wandb
einops
pillow
spmd_types
attn-gym==0.0.5
# Not in upstream requirements.txt but imported unconditionally at module level by
# torchtitan/distributed/minimal_async_ep/kernels.py (reached via models/common/token_dispatcher.py).
# Attribution TT: upstream ask = lazy import. Plain `triton` is a pure-Python package on
# aarch64; it is NOT Triton-Ascend (which is installed separately for L1 kernels).
triton
